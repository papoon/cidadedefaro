# agents/

Custom subagent definitions for this project. Each agent is a Markdown file with frontmatter (`name`, `description`, `tools`, optional `model`) defining a specialized subagent scoped to this repo.

No custom agents defined yet.